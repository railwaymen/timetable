# frozen_string_literal: true

json.extract! device, :id, :category, :brand, :model, :serial_number, :year_of_production, :year_bought, :used_since, :price, :cpu, :ram, :device_type, :user_id, :category, :storage, :os_version, :state, :note, :user, :archived
